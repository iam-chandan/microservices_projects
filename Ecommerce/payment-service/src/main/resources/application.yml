server:
  port: 8087
spring:
  application:
    name: payment-service

  profiles:
    active: dev

  config:
    import: configserver:http://localhost:8888

  cloud:
    config:
      fail-fast: true
